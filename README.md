# Wendum parsing
Wi-Fi/LAN gate on ESP32 to parse data from cryostorage with Wendum fill and control module, simple web server to expose parameters of the cryostorage. Connection to Wendum via Wi-Fi, web server is avalable on LAN W5500 chip (port 502, like Modbus TCP).
